<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="free.mathtraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LivretActivity"
            android:label="@string/title_activity_livret"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CalculActivity" />
        <activity android:name=".result_activity" />
        <activity android:name=".config_calcul_activity"></activity>
    </application>

</manifest>